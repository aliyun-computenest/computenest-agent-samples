# 复制为 .env 后填写：cp .env.example .env

# ========== 模型（必填）==========
# 与百炼联网搜索 MCP 使用同一套百炼 API Key（sk- 开头）
# DASHSCOPE_API_KEY=sk-xxx

# 对话模型，默认 qwen-max（舆情 Deep Research 建议长上下文模型）
# DASHSCOPE_MODEL_NAME=qwen-max

# ========== 舆情编排（一般不用填）==========
# OPINION_SEARCH_TOOL_NAME=
#   含义：强制指定「联网搜索」对应的 MCP 工具 function.name（字符串，区分大小写）。
#   何时填：默认留空即可。仅当出现「未找到可用的联网搜索类 MCP 工具」、或自动选错工具时，
#   再到 百炼控制台 → MCP 广场 → 已开通的「联网搜索」→ 文档/调试里查看实际工具名后填入。
#   示例（以你控制台显示为准，勿照抄）：OPINION_SEARCH_TOOL_NAME=web_search

# ========== 会话存储 ==========
# SESSION_TYPE=json
# SESSION_TYPE=redis 时需设置：
# SESSION_REDIS_URL=redis://localhost:6379/0
