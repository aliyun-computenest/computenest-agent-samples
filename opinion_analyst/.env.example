# cp .env.example .env

DASHSCOPE_API_KEY=
# DASHSCOPE_MODEL_NAME=qwen3.7-max

# Redis（会话存储，必填）
SESSION_REDIS_URL=redis://localhost:6379/0

HOST=0.0.0.0
PORT=8090

# 设为 1 时启动预建 Session「Default」（默认 0；容器部署为 1）
CREATE_DEFAULT_SESSION=0
