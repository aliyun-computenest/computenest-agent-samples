# 环境变量（.env）
python-dotenv>=1.0.0

# AgentScope 框架与 Runtime
agentscope>=1.0.0
agentscope-runtime>=1.1.0

# Redis 客户端（当 SESSION_TYPE=redis 时使用）
redis>=5.0.0

agentrun-sdk >= 0.0.21

# 测试客户端
httpx>=0.27.0
